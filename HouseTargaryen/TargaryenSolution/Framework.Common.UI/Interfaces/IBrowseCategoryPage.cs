namespace Framework.Common.UI.Interfaces
{
    /// <summary>
    /// This is for the browse category pages on the front page, don't know if we want to enforce any methods, this is more a way of tagging pages
    /// that can be used for the generics for that method so someone doesn't do anything weird with it.
    /// </summary>
    public interface IBrowseCategoryPage : ICreatablePageObject
    {
    }
}